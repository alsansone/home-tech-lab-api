#!/bin/bash

# Always run from the project root
cd "$(dirname "$0")/.."

# Load NVM so pm2 is in path
export NVM_DIR="$HOME/.nvm"
source "$NVM_DIR/nvm.sh"

echo "🔄 Building API..."
npm run build

echo "🚀 Restarting API with PM2..."
pm2 restart home-tech-lab-api

echo "✅ API deployed successfully."